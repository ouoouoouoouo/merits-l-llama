# PyTorch — RTX 4090 (Ada Lovelace sm_89) uses stable torch 2.6 cu124.
# For Blackwell (sm_120) you would need torch 2.7+ / cu128 nightly instead.
#
# All three torch* packages must be pinned to +cu124 versions from the PyTorch
# index. Without an explicit version, pip's resolver may prefer newer PyPI
# builds of torchaudio that link against CUDA 13 (libcudart.so.13), which
# does not exist on cu124 systems.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# LLM + PEFT (pinned to versions that co-exist with torch 2.6)
transformers==4.46.3
peft==0.13.2
accelerate==1.1.1
bitsandbytes==0.44.1

# Data / training
datasets>=2.20
soundfile
librosa
scikit-learn
pandas
tqdm
pyyaml
numpy

# Experiment tracking
wandb
tensorboard
